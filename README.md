# conversor-numero-flask

Esse é um projeto em flask que retorna o número passado em formato "extenso".
Ex: 1 - `{"extenso" : "um"}`

## Como instalar as dependencias
 `pip install -r requirements.txt`

## Como rodar
 `FLASK_APP=api.py flask run`
